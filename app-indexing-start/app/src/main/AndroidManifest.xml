<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.recipe_app" >

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/CustomActionBarTheme" >

        <activity
            android:name=".client.HomeActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:theme="@android:style/Theme.Holo.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".client.RecipeActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@android:style/Theme.Holo.NoActionBar">
            <intent-filter
                android:autoVerify="true"
                android:label="@string/app_name">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Accepts URIs that begin with "http://recipe-app.com/recipe" -->
                <data
                    android:host="recipe-app.com"
                    android:pathPrefix="/recipe"
                    android:scheme="http" />
            </intent-filter>
        </activity>

        <provider
            android:name=".client.content_provider.RecipeContentProvider"
            android:authorities="com.recipe_app" >
        </provider>

        <service
            android:name=".client.AppIndexingService"
            android:exported="true"
            android:permission="com.google.android.gms.permission.APPINDEXING">
            <intent-filter>
                <action android:name="com.google.firebase.appindexing.UPDATE_INDEX" />
            </intent-filter>
        </service>
    </application>
</manifest>
<!--Step 11. Log User Actions on Public Content -->
<!--DONE-->
<!--Step 10. Remove Items From the On-device Index -->
<!--DONE-->
<!--Step 9. Update the On-device Index -->
<!--DONE-->
<!--Step 8. Write Personal Content to On-device Index -->
<!--DONE-->
<!--Step 7. Support Links For Your App Content -->
<!--DONE-->
<!--Step 6. Run the Sample App -->
<!--DONE-->
<!-- Step 5. Add the App Indexing API-->
<!--DONE-->
<!-- Step 4. Create a New Project in Firebase Console-->
<!--DONE-->
<!-- Step 3. Import the Sample App-->
<!--DONE-->
<!--Step 2. Get the Sample Code-->
<!--DONE-->
<!--Step 1. Overview-->
<!--DONE-->